@using MKERunApp.Components.Notifications

<MudDrawer DisableOverlay
           Elevation="24"
           Variant="DrawerVariant.Mini">
    <MudNavMenu>
        @foreach (var linkInfo in MKERunAppLink.AllNavLinks)
        {
            <MudNavLink Href="@linkInfo.Link" Icon="@linkInfo.Icon" />
        }
    </MudNavMenu>
    <MudSpacer />
    <NotifactionsBell />
</MudDrawer>